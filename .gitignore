node_modules
dist
dist-electron
dist-sidecar
electron/sidecar/runtime
# Bundled speaker-embedding model — fetched at package time by scripts/fetch-voice-model.mjs (~102 MB), not committed
client-aac/public/models/Xenova
# Staged on-device model runtimes/models — scripts/prepare-client-models.mjs (all client build/dev paths)
client-aac/public/ort
client-aac/public/mediapipe
client-aac/public/models/mediapipe
release
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.claude
claude.md
generative-ai
reference-files
planning-docs

# Debug logs — may contain PHI / transcripts / biometric identifiers. Never commit.
*.log
*-debug.json
hila-session-debug.json

# Private keys / credentials. Never commit.
# (rds-ca-bundle.pem is the PUBLIC AWS RDS CA bundle and is intentionally kept.)
*.pem
!rds-ca-bundle.pem