node_modules
dist
dist-electron
dist-sidecar
electron/sidecar/runtime
# Bundled speaker-embedding model — fetched at package time by scripts/fetch-voice-model.mjs (~102 MB), not committed
client-aac/public/models/Xenova
# Staged on-device model runtimes/models — scripts/prepare-client-models.mjs (all client build/dev paths)
client-aac/public/ort
client-aac/public/mediapipe
client-aac/public/models/mediapipe
# Kokoro local neural TTS voice (~94 MB) — scripts/fetch-kokoro-model.mjs
client-aac/public/models/onnx-community
release
# Generated iOS native project — emitted by `npx cap add ios` on a macOS
# runner during release. Everything we customize about it lives in
# capacitor.config.ts + scripts/ios-configure.mjs, so the ~200-file Xcode
# project is disposable and stays out of the repo.
ios
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.claude
claude.md
generative-ai
reference-files
planning-docs

# Debug logs — may contain PHI / transcripts / biometric identifiers. Never commit.
*.log
*-debug.json
hila-session-debug.json

# Private keys / credentials. Never commit.
# (rds-ca-bundle.pem is the PUBLIC AWS RDS CA bundle and is intentionally kept.)
*.pem
!rds-ca-bundle.pem

# Star-flash trap output (games/world-lab/DEBUG-STAR-FLASH.md)
games/world-lab/flash-out/

# Fast-boot dev server bundle (scripts/dev-server.mjs)
.dev
