/**
 * economy-json.ts — SHIM. The boot gate moved into the shared engine
 * (shared/engine/modules/economy/json.ts) with the compiler it guards.
 */
export { parseEconomyDoc } from "@shared/engine/modules/economy/json";
